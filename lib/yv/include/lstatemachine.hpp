#pragma once

class lstatemachine {
    /// the number of actions
    int actions_size;
    /// the number of transitions
    int transitions_size;
    /// the number of states
    int states_size;
    // TODO continue
};
